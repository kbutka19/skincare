import { window } from "rxjs/operator/window";

export function showModalDataNotImplemented(htmlEvent?: Event) {
   this. dialog.open('DialogOverviewExampleDialog', {
        width: '250px',
        data: {name: this.name, animal: this.animal},
      });
}